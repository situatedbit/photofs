require 'photofs/cli'

describe 'cli integration' do
  describe :tagging do
    it 'should result in an image added to the tag'

    context 'when the image is not in the library' do
      it 'will throw an error'
    end
  end
end
